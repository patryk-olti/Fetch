import React from "react";

const User = ({ data }) => {

    const styles = {
        fontSize: '20px',
        textTransform: 'capitalize'
    }

    return(
        <div style={styles} >
                1
        </div>
    )
}

export default User;